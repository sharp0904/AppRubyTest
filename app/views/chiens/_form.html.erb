<%= form_for(@chien) do |f| %>
  <% if @chien.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chien.errors.count, "error") %> prohibited this chien from being saved:</h2>

      <ul>
      <% @chien.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :race %><br>
    <%= f.text_field :race %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
